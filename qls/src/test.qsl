use java lang.

me
	global i to 2.
	
	create new cmd whil
		
		while i < 9 then
			set i to i + 1.
		end
	
		println(('i = w(i<9)i++ = ' & (i + 0)))->System,out.
	end
	
	create new cmd plus
	
		set i to i + 1.
		println(('i = i+1       = ' & (i + 0)))->System,out.
	end
	
	create new cmd star

		set i to i * 2.
		println(('i = i*2       = ' & (i + 0)))->System,out.
	end	

	main
		proceed whil.
		proceed plus.
		proceed star.
		println('i             = ?')->System,out.
	end
end
